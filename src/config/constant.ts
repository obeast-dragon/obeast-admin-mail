// ? 全局不动配置项 只做导出不做修改

// * 首页地址（默认）
export const HOME_URL: string = "/home";

// * 登录页地址（默认）
export const LOGIN_URL: string = "/login";

// * 默认主题颜色
export const DEFAULT_PRIMARY: string = "#009688";

// * 路由白名单地址（必须是本地存在的路由 staticRouter.ts）
export const ROUTER_WHITE_LIST: string[] = ["/500"];

// * 高德地图 key
export const AMAP_MAP_KEY: string = "";

// * 百度地图 key
export const BAIDU_MAP_KEY: string = "";

// 登录请求token url
export const OAUTH_TOKEN: string = "/oauth2/token";

// * auth client details
export const CLIENTID_AND_SECRET: string = "obeast:obeast";

// aes 加密全局key
export const AES_KEY: string = "obeastAdmin-view";
